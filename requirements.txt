# Requirements for TPU VM (Python 3.10): CPU-only PyTorch + PyTorch/XLA 2.7
# Force CPU wheels for torch/vision/audio; still use PyPI for everything else.
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

# --- Core (CPU-only PyTorch + TPU backend) ---
torch==2.7.1
torchvision==0.22.1
torchaudio==2.7.1
torch-xla[tpu]==2.7.0

# --- Common libs ---
numpy==1.26.4
timm==1.0.15
wandb==0.16.6
tensorboard==2.15.1

# --- Datasets (match your existing stack) ---
datasets==4.2.0
tensorflow-cpu==2.15.1
tensorflow-datasets==4.9.3
